# Booklist

## Pydantic

- with dataclasses, we need to pass in exact arguments, and we have no way to checking if the argument is the right type we expect
- with pydantic we validate each item, making sure that the field are in the required format.
